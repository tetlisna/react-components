export type FormData = {
  firstName: string;
  lastName: string;
  age: number;
  gender: string;
  email: string;
  password: string;
  passwordConfirm: string;
  checkbox?: boolean;
  image: string;
  autocomplete: string;
};
